// import y from 'uuidv4';

// export default function generateUniqueUUID() {
//   const chars = "ABCDEFGHIJKLMNOPQRSTUVWXYZ0123456789";
//   let result = "";
//   const uuid = y.uuid().replace(/-/g, "").toUpperCase(); // Convert to uppercase and remove dashes

//   for (const char of uuid) {
//     if (chars.includes(char)) {
//       result += char;
//       if (result.length === 16) break;
//     }
//   }
//   return result;
// }

// console.log(generateUniqueUUID());
